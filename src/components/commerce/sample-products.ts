/**
 * Renders the sample products interface and its user interactions.
 */

import { storefrontProducts } from "@/features/catalog/product-catalog";

export const sampleProducts = storefrontProducts;
