# Phase 1 Handoff

Snapshot date: July 16, 2026 (America/New_York).

This document is the starting point for another engineer joining the current
branch. It records deployed shared state, the safety boundaries that must remain
in place, and the next incomplete work. Re-run the listed read-only commands
before relying on counts because Square and the database can change after this
snapshot.

The program-level phases, dependencies, and release gates are maintained in
`docs/MASTER_ROADMAP.md`. When an older document disagrees about current status,
use the master roadmap and this dated handoff, then verify external state with
the read-only commands below.

## Current shared state

- The six committed Prisma migrations are applied to the shared Supabase
  PostgreSQL database.
- All 24 operational check constraints are present, validated, and have zero
  known violations.
- The two storefront locations are mapped to active Square Production
  locations:
  - `store-3rd-avenue` -> `LP9N7FFH78H2W`
  - `store-86th-street` -> `LPTVETSP8A546`
- The initial read-only Production backfill persisted 66,141 active items,
  74,640 active variations, and 223,920 per-location inventory rows. Incremental
  syncs use Square timestamps.
- The latest checkout-readiness audit found 585 eligible products at 3rd Avenue
  and 618 at 86th Street.
- A temporary publication and rollback were exercised successfully. The current
  runtime publication is version 3 with an empty storefront payload. Version 4
  is an unpublished DRAFT with shipping removed and 751 ready placements. The
  original imported DRAFT remains immutable in version history.
- The application is not deployed publicly yet.

## Safety boundaries

- Square Production access is read-only. Do not enable catalog, inventory,
  order, or payment mutations without a separate reviewed plan and explicit
  approval.
- Checkout is `validation_only`: it revalidates server-side price and fresh
  per-location inventory, records an idempotent attempt, and creates no Square
  order or payment.
- Merchandising publication writes only to Supabase CMS/versioning tables. It
  does not write to Square. Publication and rollback both require a digest-bound
  confirmation generated by an audit command.
- Never commit `.env`, `.env.local`, access tokens, database credentials, local
  Square caches, generated reports, or files under `tmp/`.
- Do not expose `/admin` or `/api/admin/*` publicly until a production identity
  provider is selected and verified.

## Read-only operational checks

Run from the repository root:

```bash
npm ci
npx prisma migrate status
npm run audit:constraints
npm run sync:square:postgres:readonly -- --status
npm run sync:square:postgres:readonly -- --checkout-readiness
```

The root `postinstall` runs `prisma generate`, so a clean `npm ci` must leave a
generated Prisma Client ready for typechecking. If client generation fails, the
installation is not a successful reproduction and must not be treated as one.

The Square commands require the ignored local environment values documented in
`docs/environments.md`, including the Production read-only kill switch. The
readiness command exits nonzero while the reviewed content remains a DRAFT or
the current PUBLISHED payload is intentionally empty; inspect its per-location
results rather than interpreting that state as a sync failure.

## Local quality gates

```bash
npm run check
npm run build
npm run test:e2e
```

At this snapshot, a clean `npm ci` completed with Prisma Client generation and
zero dependency vulnerabilities. Typechecking and the production build passed;
the unit suite passed 135 tests across 44 files; and Playwright passed 8 tests
across desktop and mobile projects. Playwright uses the stable Webpack dev server
because the Turbopack server can panic nondeterministically after a production
build. ESLint reported 46 warnings within the documented budget of 51; new work
must not increase it.

The 2026-07-16 operational recheck found all six migrations current, all 24
constraints validated with zero violations, and the read-only incremental Square
sync completed with 185 catalog changes and 236 inventory changes. Checkout
readiness remained 585 eligible products at 3rd Avenue and 618 at 86th Street.
The audit exits nonzero only because DRAFT 4 remains intentionally unpublished.

Phase 1 closed on 2026-07-16. Branch `codex/phase-1-real-catalog` is published,
draft PR #2 records the complete Phase 0/1 diff, and remote CI run #2 passed the
quality, production build, and browser jobs. The owner accepted closure and
directed the project to proceed to Phase 2 without publishing DRAFT 4.

## Safe merchandising workflow

Audit commands do not mutate data:

```bash
npm run prepare:merchandising:no-shipping
npm run publish:merchandising
npm run rollback:merchandising
```

Each apply command requires the exact confirmation printed by its corresponding
audit. Do not publish version 4 merely to begin development; local storefront
work and tests can continue while it remains a DRAFT. See
`docs/square-integration.md` for the full commands and invariants.

## Incomplete production work

1. Configure `WEBHOOK_WORKER_SECRET`, deploy, and schedule the catalog and
   webhook workers.
2. Complete delivery zones, fees, slots, capacities, lead times, and cutoff
   rules for pickup and local delivery.
3. Approve and map the warehouse before restoring shipping availability.
4. Add and verify a production admin identity provider.
5. Keep no-shipping DRAFT 4 unpublished during Phase 1 closure and the orderly
   start of Phase 2, per the owner's 2026-07-16 decision. Any future publication
   requires a separate explicit approval.
6. Design Square order creation and Payments as a separate guarded phase. Tests
   that could charge a card must use Square Sandbox, never Production.

## Where to continue

- Square and publication runbook: `docs/square-integration.md`
- Environment matrix: `docs/environments.md`
- Deployment gates: `docs/deployment.md`
- Security boundaries: `docs/security.md`
- Schema and migrations: `prisma/schema.prisma` and `prisma/migrations/`
- Checkout behavior: `src/server/checkout/checkout-service.ts`
- Durable Square sync: `src/server/square/`
- Merchandising publication and rollback: `src/server/admin/website-merchandising-publication.ts`
- Browser coverage: `src/tests/e2e/`
